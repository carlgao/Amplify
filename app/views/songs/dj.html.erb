<div class="container">

<div id="home-box" class="box">
  <div id="title">DJ Station</div>
<!--<h2>Now Playing</h2>
<% song = @now_playing_song %>
<% if song == nil %>
  <tr>No songs currently playing</tr>
<% else %>
 <table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Requests</th>
    <th>Upvotes</th>
    <th>Downvotes</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <tr>
    <td><%= song.id %></td>
    <td><%= song.name %></td>
    <td><%= song.requests %></td>
    <td><%= song.upvotes %></td>
    <td><%= song.downvotes %></td>
    <td><%= song.playing %></td>
    <td><%= link_to 'Show', song %></td>
    <td><%= link_to 'Edit', edit_song_path(song) %></td>
    <td><%= link_to 'Destroy', song, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
<br />
<br />-->
<h2>Song List</h2>
<table>
  <tr>
    <th>Name</th>
    <th>Requests</th>
    <th>Upvotes</th>
    <th>Downvotes</th>
    <th>Now playing</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @songs.each do |song| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= song.requests %></td>
    <td><%= song.upvotes %></td>
    <td><%= song.downvotes %></td>
    <td><%= song.playing %></td>
    <td><%= link_to 'Show', song %></td>
    <td><%= link_to 'Edit', edit_song_path(song) %></td>
    <td><%= link_to 'Destroy', song, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
  <tr>
  <td>
    <div class="song">
      <!--<div class="song-title"><%= song.name %></div>-->
      <audio id="<%= song.id %>" controls="controls">
      <source src="<%= song.source %>" type="audio/mpeg" />
      Your browser does not support the audio element.
      </audio>
    </div>
  </td>
  <!--<td><%= song.source %></td>-->
  </tr>
<% end %>
</table>
  
  <!--<h2>New Song</h2>
  Title: <br />
  <input id="song-title" placeholder="Party Rock Anthem"/>  MP3 link: <br />
  <input id="song-link" placeholder="http://example.com/partyrock.mp3"/> <br />
  <br />

  <button id="song-add" class="btn primary">Add New Song</button>
  <br />
  <br />
  <br />-->







<%= link_to 'New Song', new_song_path %>










</div>
</div>

</div>
