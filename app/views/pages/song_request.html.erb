
<script type="text/javascript">
	document.addEventListener("touchmove", function(event) {
		// handle one finger touches
		switch(event.targetTouches.length) {
		case 1:
			var touch = event.targetTouches[0];
			if(touch.pageX < 20) {	// go forward
				window.location = "/songs/now_playing.html";
			}
			// stop the touches from moving the screen
			event.preventDefault();
			break;
		default:
			break;
		}
	}, false);
</script>

<div class="container">
  <div id="home-box" class="box">
	<!--<a href="/pages/gallery1"><button type="button" id="gallery" class="navigation btn info">Gallery</button></a>-->
	<!--	<a href="/songs/new">NEW SONG</a> -->
	<input id="autocomplete" onkeypress="onKeyPress();"/>
	<button id="request-btn" class="btn primary">Request a song</button>
	<br />
    <div data-role="fieldcontain">
	<fieldset data-role="controlgroup">
	<label for="textinput1">
	Title
	</label>
    <input id="textinput1" placeholder="" value="" type="search" />
  </fieldset>
    </div>
  </div>
</div>
